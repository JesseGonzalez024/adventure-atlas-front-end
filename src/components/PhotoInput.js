import React from 'react'

const PhotoInput = props => (

    <div>
        <input type="text" onChange={(event) => this.handleChange(event)} name="photo" value={this.state.photos}/>
    </div>

)

export default PhotoInput